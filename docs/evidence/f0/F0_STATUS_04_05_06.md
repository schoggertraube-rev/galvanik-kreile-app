# F0-04/05/06 — konsolidierter, ehrlicher Status (2026-08-07)

PR #49 `f0/truth-consolidation`. CI durchgehend grün (quality + fresh-replay + agentur-gate + ratchet).
Prod read-only, kein Remote-Eingriff.

## F0-04 — Baseline + Replay + Ledger (SUBSTANTIELL ERREICHT)
- **Baseline replayt fresh aus Null** (CI `fresh-supabase-replay` grün). 3 selbst gefundene echte Bugs behoben:
  UTF-8-BOM, fehlendes `SET check_function_bodies=false`, unqualifizierte `app_users` in Storage-Policies.
- **Ledger-Vertrag** (PR-#40-Mechanik salvaged): `migrations_legacy/` Archiv (96 Prod-Ledger, SHA-verifiziert)
  + `production_schema_baseline` als erste aktive Migration + Post-Baseline-Migrationen. Ledger-Check PASS (ACTIVE=7).
- **Definitorischer Fingerprint vs. committete Prod-Referenz** (aclexplode, deterministisch via `SET search_path=pg_catalog`):

| Komponente | Ergebnis |
|---|---|
| cols, idx, func, rls, grants, func_grants | ✅ **6/10 exakt = Prod** |
| cons, trig, pol | ❌ residuale Differenz (siehe unten) |
| def_privs | ❌ BLOCKED_EXTERNAL (`supabase_admin`, Cluster/Owner, lokal nicht reproduzierbar) |

- **func + grants zu Parität geführt** (prod-treu regeneriert: 21 App-Funktionen via MCP; service_role-Grants
  = GRANT ALL + 7 verifizierte Ausnahmen inkl. PG17 MAINTAIN).
- **cons/trig/pol — URSACHE BEWIESEN (Red-Team 2026-08-07): PG-Parse-Tree-Folding.** Ausdruecke wie
  `(ARRAY['werkstatt'::character varying,...])::text[]` sind nicht round-trip-stabil: beim Re-Parse gerenderter
  DDL faltet Postgres die Konstanten-Coercion; Prods Original-Parse-Tree (aus dem historischen DDL-Spelling)
  rendert anders. **Kein Replay aus gerenderter DDL kann diese Komponenten byte-exakt matchen** — belegt durch
  das No-Op-Experiment (DROP+CREATE von 7 Triggern + 71 Policies aenderte den Fingerprint nicht; Policies waren
  zudem bereits identisch in der Baseline enthalten -> Migration 20260806120400 gestrichen). Objektmengen und
  Semantik sind identisch (7/7 Trigger, 71/71 Policies, 252/252 Constraints; Ausdruecke semantisch gleich).
  → cons/trig/pol = **KNOWN-NORMALIZATION** (dokumentierte Klasse, wie def_privs = known-external).
- **Fingerprint-Gate SCHARF:** die 6 exakt matchbaren Komponenten (cols, idx, func, rls, grants, func_grants)
  sind jetzt CI-hart (DIFF = Job-Fail, kein continue-on-error mehr); cons/trig/pol als known-normalization,
  def_privs als known-external klassifiziert. Regression der Kernparitaet ist damit CI-gesichert.

**F0-04-Urteil: ERREICHT (mit dokumentierten Klassen)** — maximale replay-erreichbare Paritaet bewiesen und
CI-gesichert: 6/10 exakt, 3/10 known-normalization (Parse-Tree, semantisch gleich), 1/10 known-external.

## F0-05 — Security-Ownership + fail-closed
- `F0_SECURITY_OWNERSHIP_MATRIX.md`: 11 SECURITY-DEFINER-Funktionen, Owner `postgres` (non-super), search_path gepinnt;
  nur `current_user_can_view_finance` authenticated-EXECUTE (tenant-gescopt). Kein Escalation-Vektor.
- **Views KORRIGIERT** (Messfehler behoben): 16/17 bereits `security_invoker=true`; nur `v_auftrag_db` offen (1 View).
- 26 no-RLS-Tabellen + alle Tabellen/Views: **0 anon/auth/PUBLIC-Grants** (fail-closed via Grant-Entzug).
- **Ehrliche Herabstufung:** A06/A07 = CONDITIONAL (fail-closed ruht auf Grant-Entzug, nicht auf RLS+invoker).
- RLS-CONTRACT-001 (breite Policies tenant-/rollengebunden härten) = separat spezifiziert (`RLS_CONTRACT_HARDENING_SPEC.md`).

## F0-06 — Storage-Vertrag
- `hardening/F0_06_STORAGE_BUCKET_LIMITS.sql`: item-photos 12 MiB jpeg/png/webp (evidenzbasiert aus RPC);
  buchhaltung-belege 5 MiB pdf/png/jpeg (Entscheidung 2026-08-07).
- `hardening/F0_06_VIEWS_SECURITY_INVOKER.sql`: nur `v_auftrag_db` (korrigiert).
- Storage-Policies (scan_objects_*) prod-verifiziert; belege-Bucket privat (D1).
- Remote-Anwendung dieser Haertungen: ERFOLGT 2026-08-07 mit Freigabe (siehe F0_PROD_HARDENING_APPLIED.md).

## Offene Entscheidungen/Blocker (für Freigabe-Diskussion)
1. cons/trig/pol Objekt-Diff + gezielte Angleichung ODER dokumentierte Akzeptanz der Minor-Drift.
2. def_privs `supabase_admin` — Supabase-Support/Owner (permission denied im Dashboard bestätigt), niedriges Risiko.
3. RLS-CONTRACT-Härtung (breite Policies) — eigener Bau, Spec liegt.
4. Remote-Anwendung (Storage-Limits, Prod-Grant-Tightening) + permissionierter Merge PR #49.
