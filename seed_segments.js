// Historical remote segment seed retained by name for auditability.
console.error("NOT_CONFIGURED: segment seeding requires an approved tenant-scoped migration and role contract.");
process.exitCode = 1;
