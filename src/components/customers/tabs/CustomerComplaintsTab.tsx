import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const CustomerComplaintsTab = createFoundationUnavailableComponent({
  title: "Reklamationen nicht freigegeben",
  reason: "Reklamationsdaten und Folgewirkungen sind noch nicht belegt.",
});
