import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const CustomerHistorySimilarTab = createFoundationUnavailableComponent({
  title: "Kundenhistorie nicht freigegeben",
  reason: "Ähnliche Vorgänge und Historie sind noch nicht tenant- und evidenzgesichert.",
});
