import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const RoleMatrix = createFoundationUnavailableComponent({
  title: "Rollenverwaltung nicht freigegeben",
  reason: "Die freigegebene W3-Rollenmatrix muss erst in einen prüfbaren Identitäts- und Policyvertrag übersetzt werden.",
});
