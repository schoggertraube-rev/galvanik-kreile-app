"use client";

import { FoundationUnavailable } from "@/components/foundation/FoundationUnavailable";

export function IntakeCompletionSummary(_props: unknown) {
  void _props;
  return <FoundationUnavailable />;
}
