import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const ItemsSection = createFoundationUnavailableComponent({
  title: "Teilerfassung nicht freigegeben",
  reason: "Teile und Zuordnungen benötigen einen prüfbaren Capturevertrag.",
});
