import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const InfoPopover = createFoundationUnavailableComponent({
  title: "Analyseinformation nicht freigegeben",
  reason: "Erläuterungen werden erst mit der belegten Kennzahl angezeigt.",
});
