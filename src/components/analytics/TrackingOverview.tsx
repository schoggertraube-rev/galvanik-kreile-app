import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const TrackingOverview = createFoundationUnavailableComponent({
  title: "Nutzungsanalyse nicht freigegeben",
  reason: "Telemetrie bleibt ohne Consent-, Tenant-, Retention- und Auditvertrag vollständig gesperrt.",
});
