import { X } from "lucide-react";
type StatusMailOrder = { id: string; customer?: { email?: string | null; name?: string | null } | null; customerEmail?: string | null };
export function StatusMailDrawer({ orderData, onClose }: { orderData: StatusMailOrder; onClose: () => void }) {
  const customerEmail = orderData.customer?.email || orderData.customerEmail;
  return <div className="fixed inset-0 z-[1000] bg-[rgba(26,31,46,0.42)] backdrop-blur-[8px] flex items-start justify-center pt-12 pb-12 overflow-y-auto" onClick={onClose}><div className="w-full max-w-[560px] mx-4 bg-[var(--ci-surface)] rounded-[18px] border border-[var(--ci-border)] shadow-[0_1px_2px_rgba(20,15,5,0.04),0_12px_32px_rgba(20,15,5,0.08)]" onClick={(event) => event.stopPropagation()}><div className="flex items-center justify-between px-6 py-4 border-b border-[var(--ci-border)]"><h2 className="text-lg font-medium text-[var(--ci-ink)]">Status-Update senden</h2><button onClick={onClose}><X className="w-5 h-5" /></button></div><div className="p-6 space-y-4"><p className="text-sm text-[var(--ci-ink-3)]">Empfänger: {customerEmail || "fehlt"}</p><p className="text-sm text-[var(--ci-ink-3)]">E-Mail-Versand ist bis zum sicheren Server-Command-Vertrag nicht verfügbar.</p><button disabled title="Nicht verfügbar: sicherer Server-Command-Vertrag fehlt." className="w-full py-3 rounded-xl opacity-50 cursor-not-allowed bg-[var(--ci-ink)] text-[var(--ci-surface)]">Senden nicht verfügbar</button></div></div></div>;
}
