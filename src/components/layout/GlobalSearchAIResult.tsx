import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const GlobalSearchAIResult = createFoundationUnavailableComponent({
  title: "KI-Suche nicht freigegeben",
  reason: "Suchquellen, Tenantgrenzen und KI-Evidenz sind noch nicht vollständig geprüft.",
});
