import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const PriceLinesEditor = createFoundationUnavailableComponent({
  title: "Preispositionen nicht freigegeben",
  reason: "Preise und Preisänderungen gehören bis zum Finanzvertrag zu keiner freigegebenen Rolle.",
});
