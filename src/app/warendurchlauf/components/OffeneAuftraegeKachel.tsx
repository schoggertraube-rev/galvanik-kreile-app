import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const OffeneAuftraegeKachel = createFoundationUnavailableComponent({
  title: "Offene Aufträge nicht freigegeben",
  reason: "Der Prozessvertrag ist für diese Darstellung noch nicht vollständig belegt.",
});
