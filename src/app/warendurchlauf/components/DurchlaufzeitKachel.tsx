import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const DurchlaufzeitKachel = createFoundationUnavailableComponent({
  title: "Durchlaufzeit nicht freigegeben",
  reason: "Historie und Prozessereignisse sind für diese Kennzahl noch nicht beweisbar.",
});
