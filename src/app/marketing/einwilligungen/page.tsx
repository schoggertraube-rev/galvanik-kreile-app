import { FoundationUnavailable } from "@/components/foundation/FoundationUnavailable";

export default function EinwilligungenPage() {
  return <FoundationUnavailable title="Einwilligungen nicht freigegeben" reason="Consent-Daten und ihre Kunden- und Tenantzuordnung sind noch nicht gesichert." />;
}
