import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const StudioView = createFoundationUnavailableComponent({
  title: "Marketingstudio nicht freigegeben",
  reason: "Die zugrunde liegenden Kampagnen- und Einwilligungsdaten sind nicht freigegeben.",
});
