import { createFoundationUnavailableComponent } from "@/components/foundation/createFoundationUnavailableComponent";

export const ExportClient = createFoundationUnavailableComponent({
  title: "Export nicht freigegeben",
  reason: "Exportrechte, Datenauswahl und Auditreceipts sind noch nicht geprüft.",
});
