// Historical remote channel seed retained by name for auditability.
console.error("NOT_CONFIGURED: channel seeding requires an approved tenant-scoped migration and role contract.");
process.exitCode = 1;
