// Historical browser script retained by name for auditability.
// Verified browser smoke tests live in the repository test suite, not in an unscoped localhost script.
console.error("NOT_CONFIGURED: test-routes.js is quarantined; no browser test was started.");
process.exitCode = 1;
